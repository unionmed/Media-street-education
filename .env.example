# Origin only; Pages CI supplies the configured site origin automatically.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Empty for a custom domain. Use a leading slash when hosted in a subdirectory.
NEXT_PUBLIC_BASE_PATH=
